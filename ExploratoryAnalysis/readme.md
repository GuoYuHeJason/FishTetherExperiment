# TS summary:
for each fish, 

# finally, ran create_shiny_app_data.R for data visualization.
-no, very unnecessary to run create_shiny_app_data.R, since just copied the data to the shiny app folder. modified the code in app.R to read the processed data folder instead.

-deleted the create_shiny_app_data.R